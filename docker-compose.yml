version: '3.8'
services:
  resume-terminal:
    build:
        context: .
        dockerfile: Dockerfile
    # Not on DockerHub
    image: laurent/resume-terminal
    container_name: resume-terminal
    ports:
      - '80:80'
